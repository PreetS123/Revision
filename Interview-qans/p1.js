/* prototype->JavaScript is a prototype based language, so whenever we create any function then js engine
         adds a prototype property to it. Basically prototype is an object where we can attach method and
          property inside it, which enables all other object to ineherit this method and property.
        
          */
            
/*
  const obj={
    name:'preeti'
  }
  obj.course="Mern";

  let obj1= Object.create(obj);
 

//   console.log(obj1);

  function abc(){
    this.name='preeti'
  }

  abc.prototype.course='mern';
  let x= new abc();
  console.log(x);

  */

   /* 
   let obj={
    name:'preeti',
    father:'jks',
    myfather(){
        console.log(this.father);
    }
   }
   Object.prototype.grandFather='Ram-Jatan-Sharma';
   console.log(obj);

   let obj1= Object.create(obj);

   console.log(obj1);
   */

   /*
   let father={
    'name':'Jayant'
   }

   Object.prototype.grandFather='Ram_Jatan';

   let child=Object.create(father);
   console.log(child);

   child.name='Gopal';

   */

    /*
   let grandFather={
    eyes:'blue'
   }
   Object.prototype.surname='Sharma';
   let father= Object.create(grandFather);
   father.eyes='Yellow';

   let child= Object.create(father);
   child.eyes='bluish';
   console.log(child);
   */

  /* Why JavaScript is a dynamic language?
     DataType of variable can be changed during run time thats why it is a dynamic language.
     

     */
    /*
     let x=2;
     x++;
     console.log(x)
     x='3';
     console.log(x)
     x=true;
     
     console.log(x)
     */
     
    /* How does javaScript determine dataType?
     JavaScript determine the  dataType depending on its value assigned.

     */

     /* What is object constructor?
        
     */

     /*
     Difference between setInterval and SetTimeOut ?
         SetTimeOut--->The setTimeOut() method execute a function after waiting a specified number of milisecond.
         SetInterval-->  The setInterval() method repeats a given function at every given time-interval.
     */

      /*
        What is ReST API?
         Representational State  Transfer Application Programming Interface.
         Rest Api allows us to perform all http methods  on it.
         Basically we can perform GET,POST,PUT,DELETE methods on it.
         
         */
        /* 
        Difference between ES6 and Normal function?
            
        */

